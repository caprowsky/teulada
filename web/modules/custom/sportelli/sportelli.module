<?php

/**
 * Implements hook_theme().
 * @param $existing
 * @param $type
 * @param $theme
 * @param $path
 * @return array
 */
function sportelli_theme($existing, $type, $theme, $path) {
  return array(
    'sportelli' => array(
      'variables' => array(
        'output' => NULL,
      ),
    ),
  );
}
